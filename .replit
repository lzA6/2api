# 这一行是核心！它告诉 Replit 应该用哪个确切的命令来启动并持续运行你的服务器。
run = "granian main:app --host 0.0.0.0 --port 8080"

# 这一行告诉 Replit 的编辑器，你的项目主要入口文件是 main.py，方便它提供帮助。
entrypoint = "main.py"

[[ports]]
localPort = 8080
externalPort = 8080

[agent]
expertMode = true
